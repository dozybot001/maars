# Execution

- **llm_executor.py**：`execute_task`，支持 Mock、流式、Agent 模式
- **artifact_resolver.py**：从依赖任务解析 input artifacts
- **agent_tools.py**：ReadArtifact、ReadFile、WriteFile、Finish、ListSkills、LoadSkill
